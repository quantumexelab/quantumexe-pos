# Copy to apps/api/.env for local, or set in Vercel Project Settings

# Neon / Vercel Postgres connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Auth
JWT_SECRET="change-me-in-production"
